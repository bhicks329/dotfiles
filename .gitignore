# ============================================
# macOS System Files
# ============================================
.DS_Store
.AppleDouble
.LSOverride
Desktop.ini
._*
Thumbs.db
.Spotlight-V100
.Trashes
.AppleDB
.AppleDesktop
.apdisk
Network Trash Folder
Temporary Items

# ============================================
# Development Files
# ============================================

# Compiled files
*.pyc
*.pyo
*.class
*.o
*.so
*.dylib

# Dependencies
venv/
node_modules/
.sass-cache/
bower_components/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ============================================
# Dotfiles - git-friendly symlinks
# ============================================
# These are symlinked by git-friendly, ignore them here
bin/branch
bin/merge
bin/pull
bin/push

# ============================================
# Personal/Local Configuration Files
# ============================================
# These should NOT be committed - they contain personal data or credentials

# Local override files (for personal customization)
.extra
.extra.fish
.gitconfig.local
.path.local
.vimrc.local
.tmux.conf.local

# Local override files (match anywhere, in case they get created inside the repo)
**/.extra
**/.extra.fish
**/.gitconfig.local
**/.path.local
**/.vimrc.local
**/.tmux.conf.local

# Common local env/credential files
.netrc
**/.netrc
.env
.env.*
**/.env
**/.env.*

# SSH keys and config (use ssh-agent, not git)
.ssh/
*.pem
*.key
*_rsa
*_dsa
*_ecdsa
*_ed25519
id_rsa*
id_dsa*
id_ecdsa*
id_ed25519*

# GPG/PGP keys
.gnupg/
*.asc
*.gpg

# ============================================
# Cloud Provider Credentials
# ============================================
# All of these can be re-authenticated with `aws configure`, `gcloud auth`, etc.

# AWS
.aws/
.boto
.s3cfg
.mrjob.conf

# Google Cloud
.config/gcloud/

# Azure
.azure/

# ============================================
# Application Credentials & Tokens
# ============================================
# These can be re-authenticated with the respective CLI tools

# GitHub CLI (can re-auth with `gh auth login`)
.config/gh/

# SOPS/age encryption keys (generate new or restore from backup)
.config/sops/

# Firebase (can re-auth with `firebase login`)
.config/firebase/
.config/configstore/firebase-tools.json

# Docker credentials
.docker/config.json

# Kubernetes configs (can regenerate from cluster)
.kube/
.config/k9s/
.config/kubectl/

# Helm
.config/helm/

# Git credentials
.config/git/credentials

# NPM/Yarn auth tokens
.npmrc.local
.yarnrc.local

# JetBrains
.config/jgit/

# ============================================
# .config Directory - Deny by Default
# ============================================
# Block entire .config directory, then explicitly allow safe items below
.config/*

# Allow: Git's global ignore patterns (portable config)
!.config/git/
!.config/git/ignore

# Allow: Fish shell config (optional - auto-generated by conda, but portable)
!.config/fish/
!.config/fish/config.fish
.config/fish/fish_history
.config/fish/fish_variables

# ============================================
# Development Tools (auto-generated configs)
# ============================================
# These are machine-specific or auto-updated

.config/configstore/update-notifier-*.json
.config/flutter/
.config/flipperdevices.com/

# Mackup state (Mackup handles backup separately via Dropbox/iCloud)
.mackup/

# ============================================
# Build Artifacts & Caches
# ============================================
dist/
build/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# ============================================
# Editor & IDE
# ============================================
*.swp
*.swo
*~
.vscode/settings.json
.idea/

# ============================================
# Backup directories
# ============================================
# Created by setup/backup.sh - these are local backups only
backup/
*.backup
*.bak

# ============================================
# Test & Temporary Files
# ============================================
*.tmp
.cache/
tmp/
temp/
